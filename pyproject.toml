[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aime-event-wiki"
version = "0.1.0"
description = "Evidence-first, temporally safe Event Wiki agents for AIME financial datasets"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "alembic>=1.13",
  "fastapi>=0.115",
  "jinja2>=3.1",
  "langgraph>=1.0",
  "langgraph-checkpoint-postgres>=2.0",
  "openai>=1.40",
  "psycopg[binary]>=3.2",
  "pydantic>=2.8",
  "pydantic-settings>=2.4",
  "python-multipart>=0.0.9",
  "sqlalchemy>=2.0",
  "typer>=0.12",
  "uvicorn>=0.30",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.27",
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.6",
]

[project.scripts]
event-wiki = "event_wiki.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/event_wiki"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["postgres: requires a PostgreSQL instance"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
