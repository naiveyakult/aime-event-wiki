# Contributing

Use one issue and branch per task: `feat/tNN-short-name`. Write the contract test first, keep
all fixtures synthetic, and open a pull request against `main`. A pull request must include
targeted tests, the full test result, manual review-UI notes when applicable, and confirmation
that the staged diff contains no private data.
