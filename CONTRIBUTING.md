# 贡献指南

每个任务使用一个独立 Issue 和分支，分支命名为 `feat/tNN-short-name`。优先编写契约测试，所有测试夹具必须完全合成，并向 `main` 分支提交 Pull Request。

Pull Request 必须包含针对性测试、全量测试结果、适用时的审核页面手工验证记录，以及暂存差异不含任何私有数据的确认。

仓库中所有 Markdown 文档和 Agent Prompt 都必须使用中文编写；代码标识符、Schema 字段名、枚举值和命令保持原样。
